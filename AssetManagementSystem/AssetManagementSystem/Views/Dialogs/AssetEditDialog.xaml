<Window x:Class="AssetManagementSystem.Views.Dialogs.AssetEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="450" Width="600"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Title}" FontSize="18" FontWeight="Bold"/>

        <StackPanel Grid.Row="1" Margin="0 16">
            <TextBox materialDesign:HintAssist.Hint="Инвентарный номер" 
                     Text="{Binding Asset.InventoryNumber, UpdateSourceTrigger=PropertyChanged}"/>

            <ComboBox materialDesign:HintAssist.Hint="Тип ОС"
                      ItemsSource="{Binding AssetTypes}"
                      SelectedValue="{Binding Asset.AssetTypeId}"
                      DisplayMemberPath="Name"
                      SelectedValuePath="Id"
                      Margin="0 8"/>

            <TextBox materialDesign:HintAssist.Hint="Наименование" 
                     Text="{Binding Asset.Name, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0 8"/>

            <ComboBox materialDesign:HintAssist.Hint="Статус"
                      ItemsSource="{Binding Statuses}"
                      SelectedItem="{Binding Asset.Status}"
                      Margin="0 8"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Сохранить" Command="{Binding SaveCommand}" Width="100"/>
            <Button Content="Отмена" IsCancel="True" Margin="8 0 0 0" Width="100"/>
        </StackPanel>
    </Grid>
</Window>