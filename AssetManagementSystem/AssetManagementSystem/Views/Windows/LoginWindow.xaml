<Window x:Class="AssetManagementSystem.Views.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Вход в систему" Height="300" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <StackPanel>
            <TextBlock Text="Учет основных средств" FontSize="18" FontWeight="Bold" Margin="0 0 0 16"/>

            <TextBox materialDesign:HintAssist.Hint="Логин" 
                     Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0 0 0 8"/>

            <PasswordBox materialDesign:HintAssist.Hint="Пароль" 
                         Password="{Binding Password, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0 0 0 16"/>

            <Button Content="Войти" 
                    Command="{Binding LoginCommand}"
                    HorizontalAlignment="Stretch"
                    Margin="0 8 0 0"/>
        </StackPanel>
    </Grid>
</Window>